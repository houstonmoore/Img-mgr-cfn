namespace: ${namespace}
cfngin_bucket: ${cfngin_bucket_name}

sys_path: ./

stacks:
  VPC:
    template_path: templates/VPC.yml  # also supports json templates
    variables:
      Stackname: ${namespace}-VPC
      EnvironmentName: ${environment}

